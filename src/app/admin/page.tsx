import AdminNewContent from "@/components/admin/newContent";

export default async function AdminPage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center">
      <AdminNewContent />
    </main>
  );
}
